"""Admin Console — internal support tool (sample project).

A small Flask service used by a support team: customer lookup, order review,
report export preview, webhook testing, connectivity diagnostics, and a
login/session flow. The HTTP routes live in :mod:`admin_console.web`.
"""
